# Core
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = waybar
exec-once = awww-daemon --format xrgb
exec-once = fcitx5
exec-once = hypridle
exec-once = swaync
exec-once = hyprsunset --temperature 5000
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# First workspace
exec-once = [workspace 1] kitty

exec-once = [workspace 1] sleep 0.5 && \
  hyprctl dispatch layoutmsg "preselect r" && \
  kitty -e btm

exec-once = [workspace 1] sleep 1.0 && \
  hyprctl dispatch layoutmsg "preselect u" && \
  kitty -e peaclock

exec-once = [workspace 1] sleep 1.5 && \
  hyprctl dispatch layoutmsg "preselect r" && \
  cava


# Second workspace
exec-once = [workspace 2 silent] vesktop
exec-once = [workspace 2 silent] sleep 0.5 && \
  hyperctl dispatch layoutmsg "preselect r" && \
  zen-browser
